---
as_of: {{DATE}}
basis_commit: {{COMMIT}}
owner: {{OWNER}}
review_trigger: {{THE_EVENT_THAT_MAKES_THIS_FILE_WRONG}}
covers_paths:
  - {{PATH_GLOB}}
---

# Architecture

<!--
arc42, 12 sections. Keep only the sections that carry content; an empty section is worse than an
absent one. For a small system, 1, 3, 5, 7 and 11 carry most of the value.
Budget: 150 lines. Every non-obvious claim needs [F]/[I]/[U] plus a source and a date.

Scope of the drift check: covers_paths covers the code this document describes. The repository's
own tooling - the hooks, the checks, the agent configuration - is deliberately outside it. Tooling
changes rarely and is self-evident from the files themselves, and a knowledge document whose job is
to describe the checks would cost more structure than it earns. Nothing warns when that tooling is
deleted, and that is an accepted trade rather than an oversight.
-->

## 1. Introduction and goals

{{What the system must do, and the two or three quality goals that actually constrain design.}}

## 3. Context and scope

{{What sits outside the system boundary and how it is talked to.}}

## 5. Building block view

{{The top-level decomposition. Only the parts whose existence is not obvious from the tree.}}

## 7. Deployment view

{{Where it runs and what it needs to run.}}

## 11. Risks and technical debt

{{Known-bad things, each with the consequence of leaving it alone.}}

## 9. Architectural decisions

See `docs/decisions/`. Do not duplicate decision content here; link to it.
